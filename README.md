# FindBack
FindBack is a privacy-first lost-item recovery service. An owner registers an item and receives a printable QR tag. A finder scans the QR code without logging in, reports where the item was found or where it was left, and the owner sees the report in a private dashboard.
